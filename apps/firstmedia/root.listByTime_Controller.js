"use strict";

angular.module("MyApp").controller("root.listByTime_Controller", [
    "$state",
    function ($state) {

        var vm = this;

        Initialize();

        function Initialize() {
        };

    }
]);
